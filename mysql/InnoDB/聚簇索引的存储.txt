### 聚簇索引的存储
所有的InnoDB索引都是以B树的形式存储的，并且所有的表行都存储在叶子页中，页的大小默认是16KB。
当插入新记录时，InnoDB会保留1/16页空间用于将来的插入和更新。不过也分情况：
1. 如果是顺序插入，填充因子为15/16
2. 如果是随机插入，填充因子为1/2~15/16
3. 如果填充因子小于1/2，InnoDB会合并对应的节点

InnoDB不支持变更页大小。